# Conditional ML follow-up boundary

The next study is authorized only when `RES-20260726-SMT-OVERREACTION-RECLAIM-001` records zero main MSS survivors.

It may not tune the existing displacement, attention, overreaction, MSS, target, invalidation, latency, cost, risk or leverage thresholds. It changes the decision layer: a model observes only information available at MSS confirmation and decides whether the confirmed setup has positive executable expectancy after 24 bp.

The target concept remains explainable to an SMC/ICT trader:

- BTC displacement defines the impulse;
- SOL/XRP beta-residual extension defines the external-liquidity run and SMT divergence;
- residual contraction plus opposite aggressor flow defines CISD/MSS;
- ML distinguishes genuine exhaustion/reclaim from continuation through the liquidity pool;
- equilibrium reclaim and structural invalidation remain fixed.

If the rule screen has a main survivor, the required next step is exact Bybit BBO/depth and funding validation instead, and this ML follow-up must remain unopened.

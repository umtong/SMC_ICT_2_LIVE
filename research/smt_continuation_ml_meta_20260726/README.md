# ML SMT overdelivery–pullback continuation

Claim: `CLM-20260726-1906-SMT-CONTINUATION-ML-001`

## One trader-readable mechanism

1. A completed BTC displacement defines delivery.
2. SOL or XRP extends farther than prior-only BTC beta implies and accepts external liquidity.
3. The first 50% residual pullback is the quantitative FVG/OTE mitigation.
4. One explainable ML meta-label decides whether the mitigation is likely to reaccelerate in the original direction.
5. Entry follows the original delivery. Target is 150% residual expansion; 25% residual reclaim, adverse follower displacement or loss of the BTC origin invalidates.

ML does not invent direction, the setup, the entry depth, target, stop, latency, cost, risk or leverage.

## Why this exists

The fixed four-date continuation screen produced four positive 24bp trades but all occurred on one date and collapsed after winner removal. The reversal payoff was decisively negative. This claim does not tune either result. It changes only the selection information: use a single model on a broader frozen chronology to distinguish genuine reacceleration from pullbacks that continue to reclaim.

## Minimal contract

- one standardized L2 logistic model;
- one structure-only baseline;
- one isotonic calibration map;
- no model, feature, threshold, entry, target, stop, risk or leverage grid;
- BTC signal; SOL/XRP execution; one global slot;
- exact 100/300ms latency replay at 24bp;
- 2024–2026 mechanically prohibited;
- no credentials or orders.

# Product-neutral accepted-delivery Core + causal runner

Run `python materialize.py`, then execute `materialized/main_policy.py` in the frozen canonical-data runtime. The result is a component study, not a ranked or live-authorized strategy.

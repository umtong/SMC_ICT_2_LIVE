# Causal Ridge UTC-state XRP — Bybit 2026H1 forward replica

This branch freezes an independent reconstruction of the current provisional rank-one ML route **before opening 2026 outcomes**.

The original local implementation was not committed. Accordingly, this code does not claim byte identity. It reconstructs the durable economic rule: one pooled causal Ridge model, prior-only half-year refits and calibration, XRPUSDT entries only during UTC 08:00–15:59, threshold multiplier 1.25, next-hour entry, exit when signed expected edge becomes non-positive, one global slot and no elapsed-time liquidation.

The reconstruction first reproduced a strong but lower 2024–2025 selected-development path using immutable artifact `8616632878`. It then freezes fixed base quantity, marked-notional costs, actual Bybit funding and end-period liquidation-value NAV before opening 2026H1.

`2024–2025` is not fresh OOS. `2026H1` is the first forward interval for this exact replica and is reported regardless of outcome. No credential or order path exists.

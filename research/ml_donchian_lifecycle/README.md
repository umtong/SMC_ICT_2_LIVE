# ML Donchian accepted-delivery lifecycle V1

`RES-20260730-ML-DONCHIAN-LIFECYCLE-001` fixes a real protected-origin programization error but retires the exact family. BOS-gated management changed the 2023 24-bp result from −1.10% to +19.69%, yet the same deterministic policy lost −8.98% in 2022. Robust ML policies underperformed the constant legacy policy in the chronological 2021H2 diagnostic and were negative in 2022. A positive heavy-tail HGBT path was winner-dependent and selection-sensitive, so already-seen 2024H1 evidence was not promoted and the ranking is unchanged.

Reproduce with:

```bash
CANONICAL_CORE_ROOT=/path/to/core_tables \
LIFECYCLE_OUTPUT=$PWD/out \
PYTHON=/path/to/python3 \
./run_all.sh
```

See `REPORT.md`, `CONTRACT.json`, and `RESULT.json` after materializing the deterministic source bundle.
# Instantiate another project

Use `scripts/init_project.py` to rewrite project-specific identifiers. Create or select a Drive root folder, put its private ID in `config/project.local.toml`, create the standard Drive layout, then commit project configuration on a bootstrap branch. Keep runtime instructions free of model names, subscription details, and historical rationale.

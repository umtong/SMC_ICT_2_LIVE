# Research lane prompt

Read `config/project.toml`, project instructions, current state, the assigned task, and the data/source registry before external search. Work only within the assigned scope and branch. Reuse registered sources; register new durable sources once. Complete executable work rather than returning a plan. At goal completion or time limit, create an append-only run report, commit reproducible changes, open a draft PR when repository files changed, and propose the next exact task without modifying shared state.

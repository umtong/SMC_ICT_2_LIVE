name: Cross-venue price-discovery tracked V5D

on:
  pull_request:
    branches: [main]
    paths:
      - "research/cross_venue_price_discovery_20260725/**"
      - ".github/workflows/cross-venue-price-discovery-v5.yml"
      - ".github/workflows/cross-venue-price-discovery-v5b.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  authoritative-v5d:
    uses: ./.github/workflows/cross-venue-price-discovery-v5b.yml

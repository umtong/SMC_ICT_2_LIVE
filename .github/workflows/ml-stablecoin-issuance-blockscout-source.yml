name: Stablecoin Blockscout source gate legacy manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Preserve single authoritative source route
        run: |
          echo 'The authoritative corrected source gate is ml-stablecoin-blockscout-macos-source.yml.'
          echo 'This legacy workflow opens no source, market outcome, model, PnL or order path.'

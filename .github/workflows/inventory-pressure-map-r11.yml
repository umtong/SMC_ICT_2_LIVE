name: Bybit vulnerable-inventory pressure-map fatal screen

on:
  pull_request:
    branches: [main]
    paths:
      - "research/inventory_pressure_map_20260726/**"
      - ".github/workflows/inventory-pressure-map-r11.yml"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: inventory-pressure-map-refs/heads/agent/r11-inventory-pressure-map-001
  cancel-in-progress: true

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    env:
      PYTHONHASHSEED: "0"
      OMP_NUM_THREADS: "2"
      OPENBLAS_NUM_THREADS: "2"
      MKL_NUM_THREADS: "2"
      NUMEXPR_NUM_THREADS: "2"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install frozen research environment
        run: |
          python -m pip install --upgrade "pip==25.1.1"
          python -m pip install \
            "numpy==2.1.3" \
            "pandas==2.2.3" \
            "requests==2.32.4" \
            "pytest==8.3.4"

      - name: Compile, test and run causal self-test
        run: |
          set -euo pipefail
          root=research/inventory_pressure_map_20260726
          python -m py_compile "$root/run_screen.py" "$root/test_run_screen.py"
          python -m pytest -q "$root/test_run_screen.py"
          mkdir -p research_runs/inventory_pressure_map/self_test
          python "$root/run_screen.py" \
            --self-test \
            --output research_runs/inventory_pressure_map/self_test \
            --cache "$RUNNER_TEMP/inventory-pressure-cache"
          (cd research_runs/inventory_pressure_map/self_test && sha256sum --check SHA256SUMS.txt)

      - name: Run frozen pre-2024 official-source fatal screen
        run: |
          set -euo pipefail
          root=research/inventory_pressure_map_20260726
          mkdir -p research_runs/inventory_pressure_map/output
          python "$root/run_screen.py" \
            --output research_runs/inventory_pressure_map/output \
            --cache "$RUNNER_TEMP/inventory-pressure-cache" \
            2>&1 | tee research_runs/inventory_pressure_map/screen.log

      - name: Verify result safety and integrity
        run: |
          set -euo pipefail
          (cd research_runs/inventory_pressure_map/output && sha256sum --check SHA256SUMS.txt)
          python - <<'PY'
          import json
          from pathlib import Path

          result = json.loads(Path("research_runs/inventory_pressure_map/output/RESULT.json").read_text())
          assert result["candidate_count"] == 1296
          assert result["source_count"] == 36
          assert result["2024_opened"] is False
          assert result["2025_or_2026_opened"] is False
          assert result["orders_submitted"] is False
          assert result["hard_validity_status"] == "PASS_INITIAL"
          assert result["status"] in {"TESTED_BELOW_GATE", "PROMISING_PRE2024_SURVIVOR"}
          print(json.dumps({
              "status": result["status"],
              "gate_pass_count": result["gate_pass_count"],
              "positive_candidate_counts": result["positive_candidate_counts"],
              "best_candidate": result["best_candidate"],
          }, sort_keys=True, default=str))
          PY
          python scripts/validate_project.py

      - name: Record immutable input hashes
        if: always()
        run: |
          set -euo pipefail
          sha256sum \
            research/inventory_pressure_map_20260726/preregistration.json \
            research/inventory_pressure_map_20260726/README.md \
            research/inventory_pressure_map_20260726/run_screen.py \
            research/inventory_pressure_map_20260726/implementation.b64.part00 \
            research/inventory_pressure_map_20260726/implementation.b64.part01 \
            research/inventory_pressure_map_20260726/test_run_screen.py \
            research/inventory_pressure_map_20260726/source_manifest.csv \
            .github/workflows/inventory-pressure-map-r11.yml \
            > research_runs/inventory_pressure_map/INPUT_SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: inventory-pressure-map-${{ github.sha }}
          path: research_runs/inventory_pressure_map
          if-no-files-found: error
          retention-days: 30
          compression-level: 6

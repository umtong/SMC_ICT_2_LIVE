name: L2 maker toxicity V2 direct-source causal research

on:
  pull_request:
    branches: [main]
    paths:
      - "research/l2_maker_toxicity_v1/**"
      - ".github/workflows/l2-maker-toxicity-v2.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: l2-maker-toxicity-v2-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  research:
    runs-on: ubuntu-24.04
    timeout-minutes: 340
    env:
      PYTHONUNBUFFERED: "1"
      HF_HUB_DISABLE_TELEMETRY: "1"
      HF_HUB_ETAG_TIMEOUT: "120"
      HF_HUB_DOWNLOAD_TIMEOUT: "600"
      ROOT: ${{ github.workspace }}/research_runs/l2_maker_toxicity_v2
      CACHE: ${{ github.workspace }}/.cache/l2_maker_toxicity_v2

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Verify immutable readable research sources
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$ROOT" "$CACHE"
          python - <<'PY'
          from __future__ import annotations
          import hashlib, json
          from pathlib import Path

          expected = {
            "research/l2_maker_toxicity_v1/run.py": {"bytes": 42056, "sha256": "ae79c0afb130cb8eef806cc0c24d7ea6aa8719f3133ccb2e3b8da8dfed51a168"},
            "research/l2_maker_toxicity_v1/test_run.py": {"bytes": 9265, "sha256": "b9f1684d1140ebc2f6c58ec908f112452b9b064049c41a61994f6bfb2abe7e66"},
            "research/l2_maker_toxicity_v1/preregistration.json": {"bytes": 4449, "sha256": "3fcb7e7bd0653f50345af12c302328a877bd39c146190dd2f2c4c5d211dde457"},
          }
          observed = {}
          for name, spec in expected.items():
              path = Path(name)
              payload = path.read_bytes()
              actual = {"bytes": len(payload), "sha256": hashlib.sha256(payload).hexdigest()}
              observed[name] = actual
              if actual != spec:
                  raise RuntimeError(f"immutable source mismatch {name}: {actual} != {spec}")
          compile(Path("research/l2_maker_toxicity_v1/run.py").read_bytes(), "run.py", "exec")
          compile(Path("research/l2_maker_toxicity_v1/test_run.py").read_bytes(), "test_run.py", "exec")
          Path("research_runs/l2_maker_toxicity_v2/source_verification.json").write_text(
              json.dumps({"expected": expected, "observed": observed, "status": "PASS"}, indent=2, sort_keys=True),
              encoding="utf-8",
          )
          print(json.dumps(observed, sort_keys=True))
          PY
          cp research/l2_maker_toxicity_v1/preregistration.json "$ROOT/preregistration.json"

      - name: Install fixed research environment
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade "pip==25.1.1"
          python -m pip install \
            "numpy==2.1.3" \
            "pandas==2.2.3" \
            "pyarrow==18.1.0" \
            "scikit-learn==1.6.1" \
            "huggingface_hub==0.27.1" \
            "hf_xet==1.1.10" \
            "pytest==8.3.4"
          python - <<'PY'
          import json, platform, sys
          import numpy, pandas, pyarrow, sklearn, huggingface_hub
          from pathlib import Path
          payload={
            "python":sys.version,"platform":platform.platform(),"machine":platform.machine(),
            "numpy":numpy.__version__,"pandas":pandas.__version__,"pyarrow":pyarrow.__version__,
            "scikit_learn":sklearn.__version__,"huggingface_hub":huggingface_hub.__version__,
            "orders_submitted":False,"paper_live_enabled":False,"terminal_dates_opened":False,
          }
          Path("research_runs/l2_maker_toxicity_v2/environment.json").write_text(json.dumps(payload,indent=2,sort_keys=True))
          PY

      - name: Run causality and execution regression tests
        shell: bash
        run: |
          set -euo pipefail
          pytest -q research/l2_maker_toxicity_v1/test_run.py 2>&1 | tee "$ROOT/unit_tests.log"

      - name: Run preregistered discovery and validation
        shell: bash
        run: |
          set -euo pipefail
          python research/l2_maker_toxicity_v1/run.py --output "$ROOT" --cache "$CACHE" 2>&1 | tee "$ROOT/run.log"

      - name: Assert safety and evidence status
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          root=Path("research_runs/l2_maker_toxicity_v2")
          payload={"orders_submitted":False,"paper_live_enabled":False,"terminal_dates_opened":False,"v1_evidence_accepted":False}
          summary=root/"summary.json"
          if summary.exists():
              value=json.loads(summary.read_text(encoding="utf-8"))
              assert not value.get("terminal_dates_opened",False)
              assert not value.get("orders_submitted",False)
              assert not value.get("paper_live_enabled",False)
              payload.update({k:value.get(k) for k in ("status","candidate_count","calibration_gate_count","validation_gate_count","target_met")})
          payload["summary_present"]=summary.exists()
          (root/"wrapper_status.json").write_text(json.dumps(payload,indent=2,sort_keys=True),encoding="utf-8")
          print(json.dumps(payload,sort_keys=True))
          PY

      - name: Record immutable output hashes
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$ROOT"
          find "$ROOT" -type f ! -name OUTPUT_SHA256SUMS.txt -print0 | sort -z | xargs -0 sha256sum > "$ROOT/OUTPUT_SHA256SUMS.txt"

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: l2-maker-toxicity-v2-${{ github.sha }}
          path: research_runs/l2_maker_toxicity_v2
          if-no-files-found: error
          retention-days: 30
          compression-level: 6

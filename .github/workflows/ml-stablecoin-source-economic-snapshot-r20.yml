name: Stablecoin pinned source and causal economic snapshot (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Superseded before source decision and market outcome.'
          echo 'The sole authoritative execution is ml-stablecoin-source-economic-pr-r20.yml on PR #263.'

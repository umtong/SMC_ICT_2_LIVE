name: YT Trinity YouTube.js transport probe
# Trigger revision 1: run after takeover PR creation.

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - research/yt_trinity_ml/probe_youtubejs_transport.mjs
      - .github/workflows/yt-trinity-youtubejs-transport-probe.yml
  push:
    branches:
      - agent/r23-yt-trinity-asr-takeover-001
    paths:
      - research/yt_trinity_ml/probe_youtubejs_transport.mjs
      - .github/workflows/yt-trinity-youtubejs-transport-probe.yml

permissions:
  contents: read

concurrency:
  group: yt-trinity-youtubejs-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/r23-yt-trinity-asr-takeover-001
          fetch-depth: 1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: research/yt_trinity_ml/package-lock.json

      - name: Install pinned transport dependencies
        working-directory: research/yt_trinity_ml
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! -f package.json ]]; then
            npm init -y >/dev/null
          fi
          npm install --no-audit --no-fund --save-exact youtubei.js@17.0.1 @nadimtuhin/ytranscript@latest
          npm ls --depth=0 > "../../dependency-${RUNNER_OS}.txt"

      - name: Probe YouTube.js transcript and public audio range
        shell: bash
        run: |
          set -o pipefail
          mkdir -p "artifact/youtubejs-${RUNNER_OS}"
          node research/yt_trinity_ml/probe_youtubejs_transport.mjs \
            "artifact/youtubejs-${RUNNER_OS}" \
            2>&1 | tee "artifact/youtubejs-${RUNNER_OS}/stdout.log"

      - name: Probe independent ytranscript CLI
        if: always()
        shell: bash
        run: |
          mkdir -p "artifact/ytranscript-${RUNNER_OS}"
          python - <<'PY'
          import json
          import os
          import pathlib
          import subprocess
          import time

          samples = [
              ("chartbro", "0h9lpMUBSlE"),
              ("swipalnam", "-Tp2fhvVVGM"),
              ("indicator_sensei", "2U0s_i07vMY"),
              ("known_previous_success", "F6wDs1HRTSo"),
          ]
          out = pathlib.Path("artifact") / f"ytranscript-{os.environ['RUNNER_OS']}"
          out.mkdir(parents=True, exist_ok=True)
          rows = []
          for channel_slug, video_id in samples:
              started = time.monotonic()
              row = {"channel_slug": channel_slug, "video_id": video_id}
              try:
                  proc = subprocess.run(
                      ["npx", "--yes", "@nadimtuhin/ytranscript", "get", video_id],
                      capture_output=True,
                      text=True,
                      timeout=120,
                      check=False,
                  )
                  row.update(
                      returncode=proc.returncode,
                      stdout=proc.stdout,
                      stderr=proc.stderr,
                      nonempty=bool(proc.stdout.strip()),
                  )
              except subprocess.TimeoutExpired as exc:
                  row.update(
                      returncode=None,
                      stdout=(exc.stdout or "") if isinstance(exc.stdout, str) else "",
                      stderr=(exc.stderr or "") if isinstance(exc.stderr, str) else "",
                      nonempty=False,
                      timeout=True,
                  )
              row["elapsed_seconds"] = round(time.monotonic() - started, 3)
              rows.append(row)
              (out / f"{video_id}.json").write_text(
                  json.dumps(row, ensure_ascii=False, indent=2) + "\n",
                  encoding="utf-8",
              )
          decision = {
              "schema_version": 1,
              "runner_os": os.environ["RUNNER_OS"],
              "recovered_video_count": sum(bool(row.get("nonempty")) for row in rows),
              "videos": rows,
          }
          decision["decision"] = (
              "TRANSPORT_RECOVERED" if decision["recovered_video_count"] else "NO_RECOVERY"
          )
          (out / "probe.json").write_text(
              json.dumps(decision, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Hash immutable evidence
        if: always()
        shell: bash
        run: |
          python - <<'PY'
          import hashlib
          import pathlib

          root = pathlib.Path("artifact")
          rows = []
          for path in sorted(p for p in root.rglob("*") if p.is_file() and p.name != "SHA256SUMS"):
              rows.append(f"{hashlib.sha256(path.read_bytes()).hexdigest()}  {path.as_posix()}")
          (root / "SHA256SUMS").write_text("\n".join(rows) + "\n", encoding="utf-8")
          PY

      - name: Upload transport evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yt-trinity-youtubejs-${{ runner.os }}-${{ github.sha }}
          path: |
            artifact
            dependency-${{ runner.os }}.txt
          if-no-files-found: error
          retention-days: 30

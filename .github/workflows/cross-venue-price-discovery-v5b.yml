name: Cross-venue price-discovery exact-arrival V5B

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cross-venue-exact-arrival-v5b-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  authoritative-v5b:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install fixed environment
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade "pip==25.1.1"
          python -m pip install "numpy==2.1.3" "pandas==2.2.3" "requests==2.32.4" "pytest==8.3.4"
          mkdir -p research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b
          python - <<'PY'
          import json, numpy, pandas, platform, requests, sys
          from pathlib import Path
          root=Path('research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b')
          (root/'environment.json').write_text(json.dumps({
              'python':sys.version,
              'platform':platform.platform(),
              'numpy':numpy.__version__,
              'pandas':pandas.__version__,
              'requests':requests.__version__,
              'credentials_used':False,
              'orders_submitted':False,
              'paper_live_started':False,
              'selection_opened':False,
              'confirmation_opened':False,
              '2026_opened':False,
          },indent=2,sort_keys=True)+'\n',encoding='utf-8')
          PY

      - name: Compile and run adversarial validity tests
        shell: bash
        run: |
          set -euo pipefail
          root=research/cross_venue_price_discovery_20260725
          python -m py_compile \
            "$root/cross_venue_pilot.py" \
            "$root/cross_venue_pilot_v2.py" \
            "$root/cross_venue_development_v2.py" \
            "$root/source_probe.py" \
            "$root/source_probe_v2.py" \
            "$root/cross_venue_execution_v5.py" \
            "$root/cross_venue_execution_v5b.py" \
            "$root/cross_venue_pilot_v5.py" \
            "$root/cross_venue_pilot_v5b.py" \
            "$root/cross_venue_development_v5.py" \
            "$root/cross_venue_development_v5b.py" \
            "$root/test_cross_venue_execution_v5.py" \
            "$root/test_cross_venue_execution_v5b.py"
          PYTHONPATH="$root" pytest -q \
            "$root/test_cross_venue_execution_v5.py" \
            "$root/test_cross_venue_execution_v5b.py" \
            2>&1 | tee research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/tests.log
          PYTHONPATH="$root" python "$root/source_probe_v2.py" --self-test \
            --output research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b \
            2>&1 | tee research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/probe_self_test.log
          python scripts/validate_project.py
          sha256sum \
            "$root/CAUSAL_EXECUTION_CORRECTION_V5.md" \
            "$root/FUNDING_BOUNDARY_CORRECTION_V5B.md" \
            "$root/cross_venue_execution_v5.py" \
            "$root/cross_venue_execution_v5b.py" \
            "$root/cross_venue_pilot_v5.py" \
            "$root/cross_venue_pilot_v5b.py" \
            "$root/cross_venue_development_v5.py" \
            "$root/cross_venue_development_v5b.py" \
            "$root/test_cross_venue_execution_v5.py" \
            "$root/test_cross_venue_execution_v5b.py" \
            > research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/INPUT_SHA256SUMS.txt

      - name: Probe public local-arrival sources
        shell: bash
        run: |
          set +e
          root=research/cross_venue_price_discovery_20260725
          PYTHONPATH="$root" python "$root/source_probe_v2.py" \
            --date 2023-07-01 \
            --output research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/probe \
            2>&1 | tee research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/probe.log
          exit 0

      - name: Run frozen V5B fatal pilot and conditional development
        shell: bash
        run: |
          set -euo pipefail
          root=research/cross_venue_price_discovery_20260725
          runroot=research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b
          usable=$(python - <<'PY'
          import json, pathlib
          p=pathlib.Path('research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b/probe/SOURCE_PROBE_V2.json')
          print('true' if p.exists() and json.loads(p.read_text())['all_required_sources_usable'] else 'false')
          PY
          )
          if [[ "$usable" != "true" ]]; then
            python - <<'PY'
          import json, pathlib
          root=pathlib.Path('research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b')
          payload={
              'schema_version':1,
              'claim_id':'CLM-20260725-1850-XVENUE-001',
              'stage':'SOURCE_UNAVAILABLE_V5B',
              'causal_version':5,
              'causal_engine_version':'5B',
              'strategy_or_pnl_computed':False,
              'development_opened':False,
              'selection_opened':False,
              'confirmation_opened':False,
              '2026_opened':False,
              'orders_submitted':False,
              'paper_live_started':False,
              'ranking_eligible':False,
          }
          (root/'V5B_SEQUENTIAL_RESULT.json').write_text(json.dumps(payload,indent=2,sort_keys=True)+'\n')
          PY
            exit 0
          fi
          PYTHONPATH="$root" python "$root/cross_venue_pilot_v5b.py" \
            --output "$runroot/pilot" \
            --cache "$RUNNER_TEMP/xvenue-v5b-cache" \
            2>&1 | tee "$runroot/pilot.log"
          PYTHONPATH="$root" python "$root/cross_venue_development_v5b.py" \
            --pilot-dir "$runroot/pilot" \
            --output "$runroot/development" \
            --cache "$RUNNER_TEMP/xvenue-v5b-cache" \
            2>&1 | tee "$runroot/development.log"
          python - <<'PY'
          import json, pathlib
          root=pathlib.Path('research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b')
          pilot=json.loads((root/'pilot'/'PILOT_RESULT.json').read_text())
          development=json.loads((root/'development'/'DEVELOPMENT_RESULT.json').read_text())
          payload={
              'schema_version':1,
              'claim_id':'CLM-20260725-1850-XVENUE-001',
              'stage':development['stage'],
              'causal_version':5,
              'causal_engine_version':'5B',
              'pilot_fatal_edge_pass_count':int(pilot.get('fatal_edge_pass_count',0)),
              'development_opened':bool(development.get('development_opened',False)),
              'development_gate_pass_count':int(development.get('development_gate_pass_count',0)),
              'selection_opened':False,
              'confirmation_opened':False,
              '2026_opened':False,
              'orders_submitted':False,
              'paper_live_started':False,
              'ranking_eligible':False,
              'earlier_engine_outputs_admissible':False,
          }
          (root/'V5B_SEQUENTIAL_RESULT.json').write_text(json.dumps(payload,indent=2,sort_keys=True)+'\n')
          print(json.dumps(payload,indent=2,sort_keys=True))
          PY

      - name: Record immutable output checksums
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          root=research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b
          find "$root" -type f ! -name OUTPUT_SHA256SUMS.txt -print0 | sort -z | xargs -0 sha256sum > "$root/OUTPUT_SHA256SUMS.txt"

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cross-venue-price-discovery-exact-arrival-v5b-${{ github.sha }}
          path: research_runs/cross_venue_price_discovery_20260725/exact_arrival_v5b
          if-no-files-found: error
          retention-days: 14
          compression-level: 6

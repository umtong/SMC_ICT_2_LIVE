name: YT Trinity public Cobalt audio fleet probe

on:
  workflow_dispatch:
  push:
    branches: [agent/r22-yt-trinity-ml-001]
    paths:
      - research/yt_trinity_ml/probe_cobalt_audio_fleet.py
      - control/yt-trinity-cobalt-audio-fleet-trigger.json
      - .github/workflows/yt-trinity-cobalt-audio-fleet.yml

permissions:
  contents: write

concurrency:
  group: yt-trinity-cobalt-audio-fleet-${{ github.ref }}
  cancel-in-progress: false

jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            key: Linux
          - os: macos-14
            key: macOS
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install probe dependency
        run: python -m pip install -q --upgrade requests
      - name: Probe live Cobalt-compatible instances
        run: |
          mkdir -p artifact/cobalt
          python research/yt_trinity_ml/probe_cobalt_audio_fleet.py \
            --output artifact/cobalt \
            --workers 16 \
            2>&1 | tee artifact/cobalt/STDOUT.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: yt-trinity-cobalt-audio-fleet-${{ matrix.key }}-${{ github.sha }}
          path: artifact/cobalt
          if-no-files-found: error
          compression-level: 6
          retention-days: 30
      - name: Persist fleet result and validated winner
        if: always()
        run: |
          result_source="artifact/cobalt/result.json"
          winner_source="artifact/cobalt/winner.json"
          result_target="research/yt_trinity_ml/COBALT_AUDIO_FLEET_${{ matrix.key }}.json"
          winner_target="research/yt_trinity_ml/COBALT_AUDIO_WINNER_${{ matrix.key }}.json"
          if [[ -f "$result_source" ]]; then
            cp "$result_source" "$result_target"
          fi
          if [[ -f "$winner_source" ]]; then
            cp "$winner_source" "$winner_target"
          elif [[ -f "$winner_target" ]]; then
            rm -f "$winner_target"
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$result_target"
          if [[ -f "$winner_target" ]]; then git add "$winner_target"; fi
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Record ${{ matrix.key }} Cobalt audio fleet probe ${GITHUB_RUN_ID}"
          for attempt in 1 2 3 4 5; do
            git pull --rebase origin "${GITHUB_REF_NAME}"
            if git push origin "HEAD:${GITHUB_REF_NAME}"; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1

name: Donchian tail superseded before outcomes

on:
  push:
    branches: [agent/r13-ml-donchian-tail-select-001]
    paths:
      - '.github/workflows/ml-donchian-tail-r13.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ml-donchian-tail-r13-authoritative
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Record objective-first stop
        run: |
          echo 'No market row, model fit, label, action, trade or PnL is opened.'
          echo 'Revision 18 requires completion of Hyperliquid liquidation, stablecoin issuance and Uniswap inventory-transfer source gates before another model family.'

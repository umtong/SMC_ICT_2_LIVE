name: Stablecoin causal V2 execution superseded before outcomes

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Superseded before source decision and market outcome.'
          echo 'The only authority is the strict-causal V3 workflow pinned to correction PR #268.'

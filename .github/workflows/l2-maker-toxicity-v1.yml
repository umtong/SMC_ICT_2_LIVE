name: L2 maker toxicity V1 INVALID

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  invalidated-transport:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse invalid V1 execution
        run: |
          echo "V1 is hard-invalid: completed-second timing, fill-conditioned arbitration, pending-slot occupancy, fixed-price acknowledgement, exact-second clock, timestamp normalization and queue-state defects were found before usable PnL evidence."
          echo "Run the hash-locked L2 maker toxicity V2 workflow instead."
          exit 1

# Copy to config/project.local.toml. This file is ignored by Git.
# Do not commit private URLs, credentials, access tokens, or restricted identifiers.

[google_drive]
root_folder_id = "REPLACE_WITH_DRIVE_FOLDER_ID"

[optional]
notes = "Local/private project binding"
